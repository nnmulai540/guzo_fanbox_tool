export const metadata = {
  title: "はじめまして",
  date: "2026-09-15",
  author: "管理人",
  excerpt: "ブログ、はじめました。",
};

はじめまして。このブログでは活動の記録を書いていきます。

- 記事は `src/content/posts/` に `.mdx` ファイルを追加するだけで増やせます
- ファイル名がそのままURL（`/blog/ファイル名`）になります
- 上の `metadata` の `title` / `date` / `author` / `excerpt` を書き換えれば、その記事の表示内容が変わります

よろしくお願いします。
